/**
 * Confess is only available in Face 1 (Home / Explore).
 * This stub exists to satisfy the Expo Router file-based routing.
 */
import { View } from 'react-native';

export default function PrivateConfessScreen() {
  return <View />;
}
